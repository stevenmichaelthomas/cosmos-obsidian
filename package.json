{
  "name": "cosmos-obsidian",
  "version": "0.4.6",
  "description": "Obsidian plugin — sync vault metadata to Cosmos",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.4.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
